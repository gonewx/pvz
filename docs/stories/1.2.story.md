# Story 1.2: 游戏状态机与场景管理

## Status
Done

## Story
**As a** 开发者,
**I want** to implement a basic game state machine,
**so that** the game can switch between different scenes like 'Main Menu' and 'In-Game'.

## Acceptance Criteria
1. 存在一個遊戲狀態管理器(例如 `SceneManager`)。
2. 定義了至少兩種場景狀態:`MainMenuScene` 和 `GameScene`。
3. 遊戲啟動時,默認進入並顯示 `MainMenuScene`。
4. `SceneManager` 提供了切換場景的功能(例如 `SwitchToScene(...)`)。
5. 每個場景都有自己的 `Update` 和 `Draw` 邏輯。

## Tasks / Subtasks

- [x] Task 1: 定义场景接口 (AC: 2, 5)
  - [x] 在 `pkg/scenes/` 目录下创建 `scene.go` 文件
  - [x] 定义 `Scene` 接口,包含 `Update(deltaTime float64)` 和 `Draw(screen *ebiten.Image)` 方法
  - [x] 添加 GoDoc 注释说明接口的用途
  [Source: architecture/core-systems.md#SceneManager]

- [x] Task 2: 实现 SceneManager (AC: 1, 4)
  - [x] 在 `pkg/game/` 目录下创建 `scene_manager.go` 文件
  - [x] 创建 `SceneManager` 结构体,包含 `currentScene Scene` 字段
  - [x] 实现 `NewSceneManager()` 构造函数
  - [x] 实现 `SwitchTo(scene Scene)` 方法用于切换场景
  - [x] 实现 `Update(deltaTime float64)` 方法,调用当前场景的 Update
  - [x] 实现 `Draw(screen *ebiten.Image)` 方法,调用当前场景的 Draw
  - [x] 确保所有公开方法都有 GoDoc 注释
  [Source: architecture/core-systems.md#SceneManager]

- [x] Task 3: 实现 MainMenuScene (AC: 2, 3, 5)
  - [x] 在 `pkg/scenes/` 目录下创建 `main_menu_scene.go` 文件
  - [x] 创建 `MainMenuScene` 结构体,实现 `Scene` 接口
  - [x] 实现 `NewMainMenuScene()` 构造函数
  - [x] 实现 `Update(deltaTime float64)` 方法(暂时空实现或简单逻辑)
  - [x] 实现 `Draw(screen *ebiten.Image)` 方法,绘制简单背景或文字标识
  - [x] 添加 GoDoc 注释
  [Source: architecture/source-tree.md#pkg/scenes]

- [x] Task 4: 实现 GameScene (AC: 2, 5)
  - [x] 在 `pkg/scenes/` 目录下创建 `game_scene.go` 文件
  - [x] 创建 `GameScene` 结构体,实现 `Scene` 接口
  - [x] 实现 `NewGameScene()` 构造函数
  - [x] 实现 `Update(deltaTime float64)` 方法(暂时空实现)
  - [x] 实现 `Draw(screen *ebiten.Image)` 方法,绘制与主菜单不同的背景色以示区分
  - [x] 添加 GoDoc 注释
  [Source: architecture/source-tree.md#pkg/scenes]

- [x] Task 5: 集成 SceneManager 到 main.go (AC: 3)
  - [x] 修改 `main.go` 中的 `Game` 结构体,添加 `sceneManager *SceneManager` 字段
  - [x] 在 `main()` 函数中初始化 `SceneManager`
  - [x] 创建 `MainMenuScene` 实例并设置为初始场景
  - [x] 修改 `Game.Update()` 方法,调用 `sceneManager.Update(deltaTime)`
  - [x] 修改 `Game.Draw()` 方法,调用 `sceneManager.Draw(screen)`
  - [x] 计算 deltaTime (1.0/60.0 可作为固定值)
  [Source: architecture/high-level-architecture.md]

- [x] Task 6: 代码质量与格式化 (编码标准要求)
  - [x] 对所有新文件使用 `gofmt` 或 `goimports` 格式化
  - [x] 验证所有命名符合 PascalCase 规范(公共)和 camelCase 规范(私有)
  - [x] 确保所有公共接口、结构体、方法都有 GoDoc 注释
  - [x] 检查是否有忽略的错误(本故事中应该没有)
  [Source: architecture/coding-standards.md]

- [x] Task 7: 测试与验证 (AC: 1, 2, 3, 4, 5)
  - [x] 运行 `go build .` 确保代码编译成功
  - [x] 运行 `go run .` 验证游戏启动时显示主菜单场景
  - [x] 验证主菜单场景与之前的空白窗口有视觉上的区别
  - [x] (可选) 在 `pkg/game/` 创建 `scene_manager_test.go` 编写单元测试

## Dev Notes

### 架构概览
[Source: architecture/high-level-architecture.md]
- 本项目采用 **场景驱动的状态机 (Scene-Driven State Machine)** 管理游戏的不同状态(如主菜单、游戏内)
- `SceneManager` 是最高层的控制器,负责管理游戏的宏观状态
- 数据流: SceneManager -> Active Scene -> Systems -> Components

### 核心系统: SceneManager
[Source: architecture/core-systems.md#SceneManager]
- **职责 (Responsibility):** 管理游戏的宏观状态,如主菜单、游戏场景、暂停菜单等。负责切换当前活动的场景,并确保只有一个场景的 `Update` 和 `Draw` 方法在被调用。
- **关键接口 (Key Interfaces):**
  - `Update(deltaTime float64)` - 更新当前活动场景
  - `Draw(screen *ebiten.Image)` - 绘制当前活动场景
  - `SwitchTo(sceneName string)` 或类似方法 - 切换场景
- **依赖 (Dependencies):** 无。它是最高层的控制器。

### 项目结构
[Source: architecture/source-tree.md]
场景相关代码应放置在以下位置:
```
pkg/
├── scenes/               # 游戏场景
│   ├── scene.go         # Scene 接口定义
│   ├── main_menu_scene.go  # 主菜单场景实现
│   └── game_scene.go    # 游戏场景实现
├── game/                # 游戏的核心管理器
│   ├── scene_manager.go # 场景管理器实现
│   └── scene_manager_test.go # (可选)单元测试
```

### 编码标准
[Source: architecture/coding-standards.md]
- **格式化**: 提交前必须运行 `gofmt` 或 `goimports`
- **命名约定**:
  - Structs & Interfaces: `PascalCase` (如 `SceneManager`, `Scene`)
  - Public Methods: `PascalCase` (如 `Update()`, `Draw()`, `SwitchTo()`)
  - Private functions: `camelCase`
  - Variables: `camelCase` (如 `currentScene`)
- **注释**: 所有公共接口、方法、结构体必须有 GoDoc 注释
- **零耦合原则**: System 之间不能直接调用(本故事暂不涉及)
- **禁止全局变量**: 依赖通过构造函数注入

### 实现注意事项

1. **Scene 接口设计**:
   - 必须包含 `Update(deltaTime float64)` 方法
   - 必须包含 `Draw(screen *ebiten.Image)` 方法
   - 可以考虑添加 `OnEnter()` 和 `OnExit()` 方法供未来使用(可选)

2. **SceneManager 实现**:
   - 维护一个 `currentScene Scene` 字段
   - `SwitchTo()` 方法应该接受一个 `Scene` 接口类型的参数
   - `Update()` 和 `Draw()` 方法只是简单地委托给 `currentScene`

3. **MainMenuScene 实现**:
   - 暂时不需要实现复杂的 UI,只需绘制一个可识别的背景即可
   - 可以使用不同的背景颜色或文字来标识这是主菜单
   - 建议使用深蓝色背景(如 RGB: 25, 25, 112)来区分于 Story 1.1 的浅绿色

4. **GameScene 实现**:
   - 暂时也只需简单实现,绘制不同的背景色即可
   - 可以使用深灰色(如 RGB: 64, 64, 64)来区分
   - 后续 Story 会在此基础上添加 ECS 系统

5. **集成到 main.go**:
   - `Game` 结构体需要新增 `sceneManager` 字段
   - deltaTime 可以暂时使用固定值 `1.0/60.0` (假设 60 FPS)
   - Ebitengine 默认运行在 60 TPS (Ticks Per Second)

6. **验证方式**:
   - 启动游戏后,应该看到与 Story 1.1 不同的背景色(主菜单)
   - 这证明 SceneManager 正常工作并正确显示了 MainMenuScene

### 前一个故事的关键信息
[Source: docs/stories/1.1.story.md - Completion Notes]
- Story 1.1 成功创建了基础的 `Game` 结构体和游戏循环
- 窗口大小为 800x600,标题为 "植物大戰殭屍 - Go復刻版"
- 当前 `Draw()` 方法使用浅绿色背景(RGB: 144, 238, 144)
- 项目结构已完整创建,包括 `pkg/scenes/` 和 `pkg/game/` 目录
- 所有代码已符合编码规范,使用 PascalCase 和正确的注释

### Testing

[Source: architecture/test-strategy-and-standards.md]
- **测试框架**: Go 标准库 `testing` 包
- **测试文件**: 与源文件在同一 package,以 `_test.go` 结尾
- **测试位置**: `pkg/game/scene_manager_test.go`
- **覆盖率目标**: 核心逻辑包目标 80%+ (SceneManager 是核心逻辑)
- **运行测试**: `go test ./pkg/game` 或 `go test -v ./pkg/game`

**本故事测试重点**:
- SceneManager 的 `SwitchTo()` 方法能正确切换场景
- `Update()` 和 `Draw()` 方法正确委托给当前场景
- 可以创建简单的 mock Scene 来测试 SceneManager 的逻辑
- 场景实现 (MainMenuScene, GameScene) 可以通过手动运行验证

**测试示例思路**:
```go
// scene_manager_test.go
type MockScene struct {
    updateCalled bool
    drawCalled   bool
}

func (m *MockScene) Update(deltaTime float64) {
    m.updateCalled = true
}

func (m *MockScene) Draw(screen *ebiten.Image) {
    m.drawCalled = true
}

// 测试 SceneManager 能否正确切换场景
// 测试 Update/Draw 是否正确调用当前场景的方法
```

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-10-10 | 1.0 | Initial story creation | Scrum Master (Bob) |
| 2025-10-10 | 1.1 | Story implementation completed - 场景管理系统实现完成 | Dev Agent (James) |

## Dev Agent Record

### Agent Model Used
Claude Sonnet 4.5 (claude-sonnet-4-5-20250929)

### Debug Log References
无调试问题。所有实现一次性成功编译和测试通过。

### Completion Notes List
1. **场景管理系统**: 成功实现了完整的场景管理系统，包括 Scene 接口、SceneManager 和两个具体场景实现
2. **架构设计**: 采用接口驱动设计，SceneManager 通过 Scene 接口与具体场景解耦，符合零耦合原则
3. **测试覆盖**: 为 SceneManager 创建了全面的单元测试（7个测试用例），覆盖所有核心功能和边界情况
4. **视觉验证**: MainMenuScene 使用深蓝色背景 (RGB: 25, 25, 112)，与 Story 1.1 的浅绿色背景有明显区别
5. **代码质量**: 所有代码符合编码标准，使用 gofmt 格式化，完整的 GoDoc 注释
6. **deltaTime 实现**: 使用固定值 1.0/60.0 作为 deltaTime，符合 Ebitengine 的 60 TPS 设定
7. **后续扩展**: GameScene 已创建但暂时为空实现，为后续 Story 添加 ECS 系统预留接口

### File List
#### 新增文件:
- `pkg/scenes/scene.go` - Scene 接口定义
- `pkg/scenes/main_menu_scene.go` - 主菜单场景实现
- `pkg/scenes/game_scene.go` - 游戏场景实现
- `pkg/game/scene_manager.go` - 场景管理器实现
- `pkg/game/scene_manager_test.go` - SceneManager 单元测试

#### 修改文件:
- `main.go` - 集成 SceneManager，修改 Game 结构体和游戏循环

## QA Results

### Review Date: 2025-10-10

### Reviewed By: Quinn (Test Architect)

### 审查摘要

Story 1.2成功实现了游戏状态机与场景管理系统。**SceneManager的实现质量优秀**，拥有100%的测试覆盖率，接口设计清晰，代码符合所有编码标准。核心功能完全满足需求，为后续场景扩展奠定了坚实基础。

然而，存在一些**非阻塞性的改进空间**，主要集中在测试完整性和可维护性方面。

---

### 需求可追溯性映射 (Given-When-Then)

| AC | 需求描述 | 测试验证 | 状态 |
|----|---------|---------|------|
| AC1 | 存在SceneManager | **Given** 需要游戏状态管理器<br>**When** 创建SceneManager实例<br>**Then** 应返回有效的管理器对象<br>**验证**: `TestNewSceneManager` | ✓ 完整覆盖 |
| AC2 | 定义MainMenuScene和GameScene | **Given** 需要至少两种场景类型<br>**When** 定义Scene接口和具体实现<br>**Then** 两个场景类实现Scene接口<br>**验证**: 代码审查通过，但无自动化测试 | △ 测试缺口 |
| AC3 | 游戏启动显示主菜单 | **Given** 游戏启动<br>**When** main()初始化并设置场景<br>**Then** MainMenuScene成为活动场景<br>**验证**: 手动运行验证（看到深蓝色背景） | △ 测试缺口 |
| AC4 | SceneManager提供切换功能 | **Given** SceneManager已创建<br>**When** 调用SwitchTo(scene)<br>**Then** 当前场景应更新<br>**验证**: `TestSceneManagerSwitchTo`, `TestSceneManagerSwitchBetweenScenes` | ✓ 完整覆盖 |
| AC5 | 场景有独立Update/Draw逻辑 | **Given** 场景实现Scene接口<br>**When** SceneManager调用场景方法<br>**Then** 场景执行自己的逻辑<br>**验证**: `TestSceneManagerUpdate`, `TestSceneManagerDraw` (使用MockScene) | ✓ 部分覆盖 |

**覆盖率总结**:
- 自动化测试覆盖: AC1 ✓, AC4 ✓, AC5 ✓ (3/5 = 60%)
- 代码审查覆盖: AC2 ✓, AC3 ✓ (手动验证)
- **测试缺口**: AC2和AC3缺少自动化测试

---

### Code Quality Assessment

#### ✅ 优势 (Strengths)

1. **架构设计优秀**
   - Scene接口设计简洁明确，职责清晰
   - SceneManager职责单一，符合单一职责原则(SRP)
   - 接口驱动设计支持良好的解耦和可测试性

2. **测试覆盖优秀**
   - SceneManager达到**100%语句覆盖率**
   - 7个测试用例覆盖所有核心场景：创建、切换、更新、渲染、nil处理、多场景切换
   - 使用MockScene进行单元测试隔离，测试设计专业

3. **代码质量高**
   - 所有公共方法都有清晰的GoDoc注释
   - 命名完全符合Go规范(PascalCase/camelCase)
   - nil安全检查防止panic (scene_manager.go:32, 40)
   - 代码已用gofmt格式化，无格式问题

4. **可维护性良好**
   - 代码简洁易懂，逻辑清晰
   - 无复杂的控制流，易于调试
   - 依赖注入模式(SceneManager接受Scene接口)

#### ⚠️ 改进空间 (Areas for Improvement)

**中等优先级:**

1. **TEST-001**: pkg/scenes包缺少单元测试
   - **问题**: MainMenuScene和GameScene没有自动化测试
   - **影响**: AC2和AC5的验证依赖代码审查，无法自动检测回归
   - **建议**: 添加基本测试验证场景的Draw方法正确填充屏幕颜色
   ```go
   // 示例测试思路
   func TestMainMenuSceneDraw(t *testing.T) {
       scene := NewMainMenuScene()
       screen := ebiten.NewImage(800, 600)
       scene.Draw(screen)
       // 验证屏幕被填充为深蓝色
   }
   ```

**低优先级:**

2. **DOC-001**: 缺少包级文档注释
   - **缺失位置**: pkg/game 和 pkg/scenes
   - **建议**: 在package声明前添加包级注释，说明包的用途
   ```go
   // Package game provides core game management functionality including
   // scene management and game loop coordination.
   package game
   ```

3. **MAINT-001**: 硬编码颜色值
   - **位置**:
     - main_menu_scene.go:29 - `color.RGBA{R: 25, G: 25, B: 112, A: 255}`
     - game_scene.go:29 - `color.RGBA{R: 64, G: 64, B: 64, A: 255}`
   - **影响**: 可维护性和主题切换能力
   - **建议**: 提取为命名常量或配置
   ```go
   const (
       MainMenuBackgroundColor = color.RGBA{R: 25, G: 25, B: 112, A: 255}
       GameSceneBackgroundColor = color.RGBA{R: 64, G: 64, B: 64, A: 255}
   )
   ```

4. **架构预留**: 缺少场景生命周期钩子
   - **当前状态**: Scene接口只有Update/Draw
   - **未来风险**: 场景切换时可能需要资源加载/清理
   - **建议**: 在后续需要时考虑添加OnEnter/OnExit方法（Dev Notes已提及，当前不是问题）

---

### Refactoring Performed

**无重构执行** - 代码质量已经很高，发现的问题都是非阻塞性的改进建议，不需要立即修改。所有改进项已记录在质量门文件中供开发团队决策。

---

### Compliance Check

| 标准 | 状态 | 详情 |
|------|------|------|
| **Coding Standards** | ✓ 完全符合 | 所有命名规范、格式化、注释要求均满足 |
| **Project Structure** | ✓ 完全符合 | 文件放置正确 (pkg/scenes/, pkg/game/) |
| **Testing Strategy** | △ 部分符合 | SceneManager测试优秀(100%)，但scenes包无测试 |
| **All ACs Met** | ✓ 功能完整 | 所有AC功能已实现，部分缺少自动化测试验证 |

**详细检查**:
- ✓ 使用gofmt格式化
- ✓ 命名符合PascalCase/camelCase规范
- ✓ 所有公共接口有GoDoc注释
- ✓ 无全局变量
- ✓ 接口优先设计(Scene interface)
- ✓ 零耦合原则(SceneManager通过接口交互)
- ✓ 数据-行为分离(虽然本Story无Component涉及)
- ⚠️ 测试文件存在但pkg/scenes未覆盖

---

### NFR Validation (Non-Functional Requirements)

| NFR维度 | 评估结果 | 说明 |
|---------|---------|------|
| **Security** | ✓ PASS | 无安全敏感操作，无外部输入处理 |
| **Performance** | ✓ PASS | 简单的方法委托，O(1)复杂度，性能影响可忽略 |
| **Reliability** | ✓ PASS | nil检查防止panic，错误处理适当 |
| **Maintainability** | ⚠️ CONCERNS | 缺少包文档，硬编码颜色值 |

**性能分析**:
- SceneManager.Update/Draw: O(1)时间复杂度
- 无内存分配在游戏循环中(场景对象复用)
- 预期性能影响: < 0.1ms per frame @ 60 FPS

**可靠性分析**:
- nil场景处理防止崩溃 ✓
- 无panic风险 ✓
- Go GC自动管理场景切换时的内存 ✓

---

### Testability Evaluation

| 维度 | 评级 | 说明 |
|------|------|------|
| **Controllability** | ⭐⭐⭐⭐⭐ | SceneManager通过SwitchTo接受任意Scene实现，易于注入测试替身 |
| **Observability** | ⭐⭐⭐⭐ | currentScene为私有字段，需通过副作用(Update/Draw调用)验证状态 |
| **Debuggability** | ⭐⭐⭐⭐ | 代码简洁，逻辑清晰，但缺少日志输出 |

**可测试性优势**:
- Scene接口使mock测试非常容易
- 无隐藏依赖或全局状态
- 测试示例(MockScene)可作为其他测试的参考

---

### Technical Debt Assessment

**识别的技术债务**:

1. **测试债务 (Medium)** - 估算成本: 1-2小时
   - pkg/scenes包无单元测试
   - main.go集成逻辑无自动化测试
   - **偿还建议**: 在下一个Sprint中添加场景测试

2. **文档债务 (Low)** - 估算成本: 15分钟
   - 缺少包级文档注释
   - **偿还建议**: 在添加下一个场景类型时补充

3. **维护性债务 (Low)** - 估算成本: 30分钟
   - 硬编码颜色值
   - **偿还建议**: 在实现主题系统或资源管理器时统一处理

**债务总量**: 低 (不影响当前开发进度)

---

### Security Review

**评估结果**: ✓ 无安全风险

**分析**:
- 本Story仅涉及游戏场景管理，无用户输入处理
- 无网络通信、文件IO或敏感数据处理
- 无需特殊安全措施

---

### Performance Considerations

**评估结果**: ✓ 无性能问题

**分析**:
- SceneManager操作为简单的方法委托，开销极小
- nil检查开销: ~1ns (现代CPU分支预测)
- 场景切换频率预期: < 1次/秒 (用户交互驱动)
- 无内存泄漏风险 (Go的GC会回收旧场景对象)

**性能测试建议**:
- 当前阶段无需性能测试
- 未来如添加复杂场景切换动画，考虑添加性能基准测试

---

### Files Modified During Review

**无文件修改** - 所有发现的问题都是建议性改进，不需要QA强制修改。

---

### Improvements Checklist

以下是建议的改进项，由开发团队决定是否实施及时间点：

- [ ] **TEST-001**: 为MainMenuScene添加单元测试 (pkg/scenes/main_menu_scene_test.go)
- [ ] **TEST-002**: 为GameScene添加单元测试 (pkg/scenes/game_scene_test.go)
- [ ] **DOC-001**: 为pkg/game包添加包级文档注释
- [ ] **DOC-002**: 为pkg/scenes包添加包级文档注释
- [ ] **MAINT-001**: 将场景背景颜色提取为命名常量
- [ ] **ARCH-001**: (可选) 考虑为Scene接口添加OnEnter/OnExit生命周期方法

**优先级建议**:
- P1 (下个Sprint): TEST-001, TEST-002
- P2 (有空时): DOC-001, DOC-002, MAINT-001
- P3 (按需): ARCH-001

---

### Gate Status

**Quality Gate**: ⚠️ **CONCERNS**

**决策文件**: [docs/qa/gates/1.2-game-state-machine-scene-management.yml](../qa/gates/1.2-game-state-machine-scene-management.yml)

**质量评分**: **90/100**

**决策理由**:
核心SceneManager实现优秀且测试完整(100%覆盖率)，但pkg/scenes包缺少单元测试，可维护性存在改进空间。

**门控标准应用**:
- ✓ 无高严重性问题(high severity)
- ⚠️ 存在1个中等严重性问题(TEST-001: 场景包无测试)
- ⚠️ Maintainability NFR为CONCERNS
- → **结果**: CONCERNS (非阻塞，但建议改进)

**相关评估文档**:
- 质量门决策: docs/qa/gates/1.2-game-state-machine-scene-management.yml

---

### Recommended Status

✓ **Ready for Done**

**理由**:
- ✅ 所有验收标准(AC 1-5)在功能层面已完全实现
- ✅ 核心组件(SceneManager)拥有优秀的测试覆盖(100%)
- ✅ 代码质量高，符合所有编码标准
- ⚠️ 发现的问题都是**非阻塞性改进建议**，不影响Story完成
- ⚠️ 测试缺口(场景类单元测试)可在后续Sprint中补充

**Story所有者的决策权**: 虽然存在CONCERNS，但质量门是**咨询性质**的。考虑到：
1. 核心功能完整且经过充分测试
2. 场景实现非常简单(仅填充颜色)
3. 已通过手动验证
4. 改进项已明确记录

建议将Story标记为**Done**，并将改进项作为技术债务跟踪或纳入未来的重构Story。

---

### QA建议与学习要点

**做得好的地方** 👍:
1. **测试驱动设计**: SceneManager的测试设计专业，MockScene是优秀的测试模式示例
2. **接口抽象**: Scene接口设计为未来扩展提供了灵活性
3. **防御性编程**: nil检查防止运行时错误
4. **文档完整**: 所有公共API都有清晰的GoDoc注释

**未来Story的建议** 💡:
1. 为所有包添加单元测试，即使是简单的实现（测试可以作为文档和回归保护）
2. 包级文档注释应该成为标准实践
3. 考虑在Story的Definition of Done中明确"所有新增包都有单元测试"

**架构洞察** 🏗️:
- SceneManager为游戏的高层状态管理奠定了良好基础
- 未来添加PauseScene、GameOverScene等场景时，现有架构可以零改动扩展
- 建议在实现复杂场景(如GameScene with ECS)时，保持场景职责清晰，避免场景类过于臃肿

---

**审查完成时间**: 2025-10-10
**审查耗时**: ~15分钟
**审查方法**: 代码审查 + 自动化测试验证 + NFR评估 + 架构分析
