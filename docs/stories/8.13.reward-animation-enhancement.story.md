# Story 8.13: 奖励动画系统行为优化与主菜单按钮

## Status

Draft

---

## Story

**As a** 玩家,
**I want** 奖励动画中卡包弹出后只有点击卡包才进入下一阶段，并且默认在奖励面板上显示主菜单按钮,
**so that** 我能更精准地操作界面，并且能方便地返回主菜单。

---

## Acceptance Criteria

### 1. 卡包等待阶段交互优化

1. **AC1**: waiting 阶段中，只有点击卡包本身或按空格键才进入 expanding 阶段
2. **AC2**: waiting 阶段中，界面其他元素（如收集阳光、查看UI等）保持可操作状态
3. **AC3**: 鼠标悬停在卡包上时切换为手形光标
4. **AC4**: 点击卡包时播放点击音效并进入展开阶段

### 2. 主菜单按钮功能

5. **AC5**: 关卡配置支持 `showMainMenuButton` 字段，默认为 true（1-1、1-2 设置为 false）
6. **AC6**: 奖励面板右上角正确显示主菜单按钮（使用 `IMAGE_SEEDCHOOSER_BUTTON2`）
7. **AC7**: 鼠标悬停主菜单按钮时切换为手形光标，显示发光效果（`IMAGE_SEEDCHOOSER_BUTTON2_GLOW`）
8. **AC8**: 点击主菜单按钮后跳转到主菜单界面

### 3. 配置与组件

9. **AC9**: `LevelConfig` 新增 `ShowMainMenuButton` 字段，默认为 true
10. **AC10**: `RewardPanelComponent` 新增 `ShowMainMenuButton` 字段
11. **AC11**: 配置常量中定义主菜单按钮位置（右上角）

### 4. 测试覆盖

12. **AC12**: 单元测试覆盖率 ≥ 80%，覆盖卡包点击检测和按钮悬浮检测逻辑

---

## Tasks / Subtasks

### Task 1: 关卡配置新增字段 (AC: 9)

- [ ] 修改 `pkg/config/level_config.go`
  - [ ] 在 `LevelConfig` 结构体中新增 `ShowMainMenuButton bool` 字段
  - [ ] 添加 YAML 标签 `yaml:"showMainMenuButton"`
  - [ ] 添加字段注释说明用途
- [ ] 修改 `applyDefaults` 函数
  - [ ] 设置 `ShowMainMenuButton` 默认值为 `true`

### Task 2: 奖励面板组件新增字段 (AC: 10)

- [ ] 修改 `pkg/components/reward_panel_component.go`
  - [ ] 新增 `ShowMainMenuButton bool` 字段
  - [ ] 添加字段注释

### Task 3: 新增配置常量 (AC: 11)

- [ ] 修改 `pkg/config/layout_config.go`
  - [ ] 新增 `RewardPanelMainMenuButtonX` 常量（右上角X位置比例）
  - [ ] 新增 `RewardPanelMainMenuButtonY` 常量（右上角Y位置比例）

### Task 4: 实现卡包点击检测 (AC: 1, 2, 4)

- [ ] 修改 `pkg/systems/reward_animation_helpers.go`
  - [ ] 新增 `isCardPackClicked(mouseX, mouseY float64) bool` 方法
  - [ ] 实现 AABB 碰撞检测（基于卡片位置和缩放后尺寸）
  - [ ] 处理植物卡片和工具图标两种情况

### Task 5: 修改 waiting 阶段点击逻辑 (AC: 1, 2, 3, 4)

- [ ] 修改 `pkg/systems/reward_animation_phases.go` 的 `updateWaitingPhase`
  - [ ] 修改点击检测逻辑：只响应卡包点击或空格键
  - [ ] 移除"任意点击进入下一阶段"的行为
  - [ ] 保留空格键作为备用操作方式

### Task 6: 实现主菜单按钮悬浮检测 (AC: 7)

- [ ] 修改 `pkg/systems/reward_panel_render_system.go`
  - [ ] 新增 `isHoveringMainMenuButton() bool` 方法
  - [ ] 计算按钮位置（右上角）
  - [ ] 实现 AABB 碰撞检测

### Task 7: 实现主菜单按钮绘制 (AC: 6, 7)

- [ ] 修改 `pkg/systems/reward_panel_render_system.go`
  - [ ] 新增 `drawMainMenuButton(screen *ebiten.Image, alpha float64)` 方法
  - [ ] 加载 `IMAGE_SEEDCHOOSER_BUTTON2` 图片
  - [ ] 悬浮时叠加 `IMAGE_SEEDCHOOSER_BUTTON2_GLOW` 发光效果
  - [ ] 绘制按钮文字（"主菜单"）
- [ ] 修改 `drawPanel` 方法
  - [ ] 当 `panel.ShowMainMenuButton` 为 true 时调用 `drawMainMenuButton`

### Task 8: 实现主菜单按钮点击响应 (AC: 8)

- [ ] 修改 `pkg/systems/reward_animation_phases.go` 的 `updateShowingPhaseInternal`
  - [ ] 新增 `isMainMenuButtonClicked(mouseX, mouseY float64) bool` 方法
  - [ ] 检测主菜单按钮点击
  - [ ] 点击后调用 `sceneManager.SwitchToMainMenu()`

### Task 9: 集成奖励面板配置传递 (AC: 5, 10)

- [ ] 修改 `pkg/systems/reward_animation_phases.go` 的 `createRewardPanel`
  - [ ] 从 `GameState` 获取当前关卡配置
  - [ ] 将 `ShowMainMenuButton` 值传递给 `RewardPanelComponent`

### Task 10: 更新关卡配置文件 (AC: 5)

- [ ] 修改 `data/levels/level-1-1.yaml`
  - [ ] 添加 `showMainMenuButton: false`（教学关卡不显示）
- [ ] 修改 `data/levels/level-1-2.yaml`
  - [ ] 添加 `showMainMenuButton: false`（教学关卡不显示）
- [ ] 其他关卡无需修改（默认值为 true）

### Task 11: 单元测试 (AC: 12)

- [ ] 创建/更新 `pkg/systems/reward_animation_system_test.go`
  - [ ] 测试 `isCardPackClicked` 方法
  - [ ] 测试卡包边界情况（边缘点击、外部点击）
- [ ] 创建/更新 `pkg/systems/reward_panel_render_system_test.go`
  - [ ] 测试 `isHoveringMainMenuButton` 方法
  - [ ] 测试 `isMainMenuButtonClicked` 方法

---

## Dev Notes

### 源代码树（相关文件）

```
pkg/
├── config/
│   ├── level_config.go          # 关卡配置结构（新增 ShowMainMenuButton 字段）
│   └── layout_config.go         # UI 布局常量（新增按钮位置常量）
├── components/
│   └── reward_panel_component.go # 奖励面板组件（新增 ShowMainMenuButton 字段）
├── systems/
│   ├── reward_animation_system.go      # 奖励动画系统主文件
│   ├── reward_animation_phases.go      # 阶段更新逻辑（修改 waiting 和 showing）
│   ├── reward_animation_helpers.go     # 辅助方法（新增卡包点击检测）
│   └── reward_panel_render_system.go   # 面板渲染（新增主菜单按钮绘制）
└── data/levels/
    └── *.yaml                   # 关卡配置文件
```

### 资源依赖

| 资源 ID | 文件名 | 说明 |
|---------|--------|------|
| `IMAGE_SEEDCHOOSER_BUTTON2` | `SeedChooser_Button2.png` | 主菜单按钮 |
| `IMAGE_SEEDCHOOSER_BUTTON2_GLOW` | `SeedChooser_Button2_Glow.png` | 主菜单按钮发光效果 |

**已在 `assets/config/resources.yaml` 中配置，无需新增。**

### 关键实现细节

#### 1. 卡包点击检测逻辑

```go
// isCardPackClicked 检查鼠标点击是否在卡包范围内
func (ras *RewardAnimationSystem) isCardPackClicked(mouseX, mouseY float64) bool {
    if ras.rewardEntity == 0 {
        return false
    }

    posComp, ok := ecs.GetComponent[*components.PositionComponent](ras.entityManager, ras.rewardEntity)
    if !ok {
        return false
    }

    // 获取卡片尺寸（考虑缩放）
    var cardWidth, cardHeight float64
    if cardComp, ok := ecs.GetComponent[*components.PlantCardComponent](ras.entityManager, ras.rewardEntity); ok && cardComp.BackgroundImage != nil {
        cardWidth = float64(cardComp.BackgroundImage.Bounds().Dx()) * cardComp.CardScale
        cardHeight = float64(cardComp.BackgroundImage.Bounds().Dy()) * cardComp.CardScale
    } else {
        // 工具奖励默认尺寸
        cardWidth = 100.0 * config.PlantCardScale
        cardHeight = 140.0 * config.PlantCardScale
    }

    // AABB 碰撞检测
    return mouseX >= posComp.X && mouseX <= posComp.X+cardWidth &&
           mouseY >= posComp.Y && mouseY <= posComp.Y+cardHeight
}
```

#### 2. waiting 阶段修改

**当前代码** (`reward_animation_phases.go:96-124`):
```go
func (ras *RewardAnimationSystem) updateWaitingPhase(dt float64, rewardComp *components.RewardAnimationComponent) {
    justPressed, _, _ := utils.IsPointerJustPressed()
    clicked := justPressed || inpututil.IsKeyJustPressed(ebiten.KeySpace)

    if clicked {
        // 进入 expanding 阶段
    }
}
```

**修改为**:
```go
func (ras *RewardAnimationSystem) updateWaitingPhase(dt float64, rewardComp *components.RewardAnimationComponent) {
    justPressed, mouseX, mouseY := utils.IsPointerJustPressed()
    spacePressed := inpututil.IsKeyJustPressed(ebiten.KeySpace)

    // 只有点击卡包本身或按空格键才进入下一阶段
    cardClicked := justPressed && ras.isCardPackClicked(float64(mouseX), float64(mouseY))

    if cardClicked || spacePressed {
        // 进入 expanding 阶段
    }
}
```

#### 3. 主菜单按钮位置

参考现有"下一关"按钮的位置计算方式，主菜单按钮位于右上角：

```go
// 建议配置值
RewardPanelMainMenuButtonX = 0.85  // 相对于背景宽度
RewardPanelMainMenuButtonY = 0.08  // 相对于背景高度
```

### ECS 架构遵循

- **组件**: 仅存储数据（`ShowMainMenuButton bool`）
- **系统**: 负责逻辑处理（点击检测、绘制）
- **零耦合**: 不直接调用其他系统，通过组件和 EntityManager 通信

[Source: docs/architecture/coding-standards.md#零耦合原则]

---

## Testing

### 测试文件位置

- `pkg/systems/reward_animation_system_test.go`
- `pkg/systems/reward_panel_render_system_test.go`

### 测试框架

使用 Go 标准库 `testing` 包。

### 测试标准

- 覆盖率目标: 80%+
- 重点测试独立的、无副作用的函数（点击检测、位置计算）

[Source: docs/architecture/testing-strategy.md]

### 关键测试用例

1. **卡包点击检测**
   - 点击卡包内部 → 返回 true
   - 点击卡包边缘 → 返回 true
   - 点击卡包外部 → 返回 false

2. **主菜单按钮悬浮检测**
   - 鼠标在按钮内 → 返回 true
   - 鼠标在按钮外 → 返回 false

3. **主菜单按钮点击检测**
   - 点击按钮内部 → 返回 true
   - 点击按钮外部 → 返回 false

---

## Change Log

| Date | Version | Description | Author |
| :--- | :--- | :--- | :--- |
| 2025-12-17 | 1.0 | 初始创建，来源于 Sprint 变更提案（*correct-course） | Bob (Scrum Master) |
| 2025-12-17 | 1.1 | PO 验证修正：ShowMainMenuButton 默认值改为 true；明确 1-1、1-2 设置为 false；修正代码行号引用 | Sarah (PO) |
